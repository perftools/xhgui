{% macro property_list(name, data) %}
<dl>
{% for k, v in data %}
    <dt>{{ k }}</dt>
    <dd class="text-wrap">
        {{ v }}
    </dd>
{% else %}
    <dt>No {{ name }} data</dt>
{% endfor %}
</dl>
{% endmacro %}
